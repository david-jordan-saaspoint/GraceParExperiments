<h1>Listing accounts</h1>

<table>
  <tr>
  	<th></th>
    <th></th>
    
    <th>Name</th>
    <th>Account number</th>
    <th>Phone</th>
  </tr>

<% @salesforce_accounts.each do |account| %>
  <tr>
  	<td><%= link_to 'Show', account %></td>
  	<td><%= link_to 'Destroy', account, :confirm => 'Are you sure?', :method => :delete %></td>
    <td><%= link_to 'Edit', edit_salesforce_account_path(h account.id) %> <%=h account.name %></td>
    <td><%=h account.id %></td>
     <td><%=h account.account_number%></td>
    <td><%=h account.phone %></td>
    
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New account', new_salesforce_account_path %>